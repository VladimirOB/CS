cd /  <<< выход к корневой папке. change directery
mkdir << путь к каталогу. если написать mrdir 123 << 123 указывает на создание.
ls << содержимое текущего каталога
vim.tiny << открыть файл << ins << чтоб печатать (это текстовый редактор)
nano << тоже редактор. /etc/bash.bashrc
vim.tiny /etc/network/interface
mv 1 f1 <<< mv 2 f2 <<< переместить папки
ln hl(hard link) ln 1.txt hl-1 Где ln (link) ссылка. -s (символическая типа ярлык)
clear << очистка
shift + : << выход // q! << выход без сохранения. wq <<< выход с сохранением.
shift +zz сохранение
cp <<< копирование. например cp 1.txt f1/
Адреса: 
10.1.214.15
Где 10.1 обязательно, 214 типо аудитория. 15 моё место.
su -l << swap user <<
rm << удаление (remote)
fdisk -l << показывает инфу о системе
less << экранный пейджер. Например less /etc/passwd или less /etc/shadow (Скрытые_) | grep at
на диске удаляется инфа когдая удаляется последняя жесткая ссылка. жесткая ссылка (275341 и т.д.)
drwxr-xr-x (d - directory. r -read. w- write. x -xzcute.) первые 3 бита для владельца. 
2-ые 3 бита для группы владельцев.
3-ие для остальных.
ntfs отличается от fat тем, что при выкл света ntfs записывает начало копирования и конец. 
acl (acces control list) исп. спец. софтом.
chmod << поменять разрешения. добавить чтение или запись для юзеров 
apt install mc << миднайн командер
apt install lynx